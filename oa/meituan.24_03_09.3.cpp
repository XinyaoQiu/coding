#include <iostream>
#include <vector>

using namespace std;

int main() {
    int n;
    cin >> n;
    vector<vector<int>> mat(n + 1, vector<int>(n + 1));
    vector<vector<int>> prefix(n + 1, vector<int>(n + 1));
    for (int i = 0)
}